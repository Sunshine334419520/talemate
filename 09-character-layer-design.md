# 人物层设计（角色卡 · 连续性 · 章末回写）

> 日期：2026-09-17
> 定位：**设计记录，不是当前实现**。含未落地部分，读的时候看 §7 的实现状态。
> 当前实现以 `08-architecture.md` 与 `src/` 代码为准（本文如与代码冲突，优先代码）。
> 比照 `04-harness-design.md`：那是 harness 的设计记录，这是人物层的。

---

## 0. 一句话

角色卡是**长期动态维护的活文档**：它要跟着小说正文一直变。因此设计的第一性问题不是"该填哪几格"，而是"**这一格随时间怎么变、谁在什么时候回写**"。

---

## 1. 为什么（现 5 格的病）

改造前的一张卡是 `# 角色：<名>` + 固定 5 个 `###`（一句话定位 / 想要·最怕 / 说话方式 / 习惯动作 / 在故事中的功能）。四个病：

1. **摘要压缩人物**。「一句话定位」写在卡首，就成了这张卡的结论——而好角色恰恰压不进一句话（见 §2 第 1 条）。
2. **没有否定式约束**。卡里没有任何"ta 绝不做 X"，模型只能猜一个人不会干什么——OOC 的头号来源。
3. **没有时间维**。角色会死、关系会换位、认知会更新，而卡里无处安放。更糟的是**没有任何机制回写**：章节落盘后没人碰 `characters/`，卡必然腐烂。
4. **关系只是一格里的修饰语**。而一场戏基本就是"两个人彼此的态度"，且**不对称才是戏**。

---

## 2. 好卡的标尺

以《十日终焉》乔家劲的人物卡为准，拆出 7 条可判据：

| # | 判据 | 例子 |
|---|---|---|
| 1 | **性格写成矛盾**，不是形容词堆 | 「虽是混混，却不愿伤人性命」 |
| 2 | **每笔都是能被用到的道具**，不是简历 | 背上纹的「天地本宽，而鄙者自隘」会在打斗脱衣时被读到。判据：这一笔在正文里什么时候会被用到？ |
| 3 | **能力即性格** | 回响契机「想要公平地进行对决」——性格被写成机制 |
| 4 | **语录是一等公民**（可验伪） | 新人写一句台词，能拿语录比出对不对；形容词比不了 |
| 5 | **关系是视角性的、复合的、且是对方声音的载体** | 荣爷既是养父又是让他代蹲四年苦窑的人（恩与欠同体）；九仔的语录被写进**乔的**卡里 |
| 6 | **弧线走向与框架判断进卡，逐章进度进细纲** | 「注定是一个悲剧式的人物」是贯穿全书的判断，每场戏都受它影响 |
| 7 | **卡是有时间的** | 「九仔：已故」「齐夏：新任大脑」 |

**这把尺子量的是决策级，不是句子级**：正确的判据是"这一格能不能改变**他在某个情境下的选择或说法**"。用句子级去量会把卡饿死（`生于1979年` 不改任何一句话，却决定他每次开口的词汇与对暴力的反射）。

**另注**：那张卡展示的是**阅读时的样子**，不是**维护时的过程**。阅读友好与维护友好是两件事——几张卡互相对不上、谁也不知道该改哪张，恰恰是"读起来舒服"的组织方式最容易出的病。所以维护结构不能照抄它的排版。

---

## 3. 卡的骨架：固定常驻带 + 开放长尾

`design/characters/<名>.md` = `# 角色：<名>` + 若干 `###` 小节。

**常驻带**（代码定，写这个角色的**任何一场戏**都要带）：

| 格 | 装什么 |
|---|---|
| 基本档案 | **逐行「键：值」**：姓名 / 性别 / 年龄段 / 身份 · 所属 / 出身（名单取「身份 · 所属」那一行） |
| 想要 · 最怕 | 欲望与恐惧——行动引擎 |
| 底线 · 绝不做 | 否定式约束，防 OOC |
| 说话方式 | 语域/口头禅/**声音范例原文**/对谁不一样 |

**按需格**（按场景切片；**按戏份决定填几格，不填不是缺陷**）：
性格与矛盾 · 来历·成因 · 语录 · 身体·习惯 · 关联角色 · 能力·机制 · 转变·走向 · 在故事中的功能

**工具托管**：`当前`（在场/已故 + 此刻处境）——点形快变状态，**普通更新一律不碰**，只由章末回写更新。

**开放长尾**：规范之外的 `###` 小节允许（世界特有的格，如「回响」），工具**原样保留**。

### 三条设计理由

- **为什么删「一句话定位」**：摘要会反过来压缩人物——它一旦写在卡首就成了结论，writer 和模型都优先按它写。另外它是**索引的需求，不是人物的需求**：名单要的是能"找到人"的东西，用事实性的 `名字 · 身份/所属` 即可。
- **为什么常驻/按需要由代码定**：开放性该由「**这一格是否每场必读**」决定，而不是由内容类型决定。必读的必须固定（否则 writer 拿不到最小保障），不必读的必须开放（否则绑死一本小说）。**分带不是分类学，是上下文预算的分配方式**：常驻格每场都付 token 所以必须小，按需格只在用到时付所以可以厚。
- **为什么"催"只催常驻格**：旧实现把「骨架有哪些格」与「哪些格必须补」焊在一处，于是建一个龙套也会被催满 12 项。拆开后：骨架固定、**填充度自由**。

### 硬约束：卡上只许 `###`

`proposal.ts` 的 `ownItems` 取**最浅**标题层。卡上冒出任意一个 `##`，全部 `###` 就都从提案审阅里消失——用户没看过的字节直接落盘。所以入参正文一律拒收标题行。

---

## 4. 点 / 边：快变量按形状切开

快变量不是同质的：

| 形状 | 例子 | 归属 |
|---|---|---|
| **点形**（属于这一个角色） | 在场 / 已故 / 处境 | **卡上的「当前」格** |
| **边形**（属于两个东西之间） | 谁知道什么（角色×秘密）、关系当前张力（角色×角色） | **`design/state/continuity.md`** |

**边形的值必须单点化**：它要跨角色保持一致，分散到 N 张卡上就是 N 份要同步的副本——"A 卡说他知道了、B 卡说他不知道"是迟早的事，而且没有任何机制会发现。**跨角色的一致性，局部文档在原理上保证不了。**

于是同一个关系有两面，各归其位：
- **卡上的「关联角色」写慢面**：账本与视角（乔欠荣爷什么、乔怎么看荣爷）。**允许两张卡不一致——不对称是素材，不是 bug。**
- **连续性文档写快面**：此刻是盟友还是反目。

`design/state/continuity.md` 只收两节：`## 关系当前张力`、`## 此刻知道什么`。
**伏笔登记不进这里**——`outline/outline.md` 已有规范小节「伏笔与回收登记」，它是规划物、归情节层。**一个事实一个家。**

---

## 5. 维护模型

按**变化频率**分三类——这是"长期动态维护"的可操作版本：

| 类 | 格 | 复核时机 |
|---|---|---|
| **不变量** | 基本档案、来历、底线 | 定了基本不动；**乱动 = 吃书**，必须过用户 |
| **慢变量** | 性格与矛盾、想要·最怕、说话方式、能力、转变·走向 | **按卷复核**（人物成长就是这几个在变） |
| **快变量** | 当前（点形）；关系张力/谁知道什么（边形） | **每章回写** |

**回写是活文档成立的前提**，也是原实现彻底缺失的一环：章节生产流程走完不碰 `characters/`，卡只会腐烂。

---

## 6. editor 在角色设计上的定位

> **一句话：editor 是"让用户笔下的人能开口"的导演——他提炼、试镜、守合同、管档案；他不造人、不填表、不写正文。**

| 定位 | 做什么 |
|---|---|
| **提炼者** | 用户带来一个模糊的人 → 帮他钉住能写的那几笔 |
| **试镜导演** | **别问"他性格是什么"，让他开口**：给一句台词，让用户听声音对不对。角色设计的验证方式是试镜而非填表——用户说"他不会这么说话"的那一刻，才真在设计角色。试镜稿是设计草稿、不是正文，所以 editor 自己给一句台词不违反"不写正文" |
| **一致性守门人** | **卡是用户签过的合同**。用户说"她这时候该心软了"而卡上写着「最怕被看穿」时，把冲突摆出来问：**改卡还是改戏？** |
| **档案管理员** | 每章定稿后做一次人物对账，把这一章引起的变化回写 |

---

## 7. 实现分期与状态

### P1 · 骨架分层 + 修数据丢失 —— ✅ 已落地（2026-09-17）

改动落在 `src/framework/characters.ts`（唯一驱动点：喂工具入参 schema、`buildCardBody`、`parseCardBody`、`pendingLabels`）：

- `CHARACTER_FIELDS` 加 `tier` 与 `aliases`；导出 `RESIDENT_FIELDS`（只有这几格会被催）与 `EDITABLE_FIELDS`（入参 schema 用，工具托管的「当前」不在内）。
- **`applyCardEdits`**：外科改（逐格 `replaceSection`），规范外小节原样保留；别名命中写回**原格**；managed 格跳过。
- **`characters/_index.md` 整个删掉**（2026-09-18）：它是卡的副本，只在角色工具那条路上重建 → 走 `propose-design` 落卡就漏（实测发生过）。名单改由 `list-designs` **现算**：一人一行 `名字 · 身份（常驻齐 / 待补：…）`，只报常驻带，不报按需格。数据源只剩卡本身，副本脱节这个问题就不存在了。
- **`##` 陷阱在三个写入口都拦住了**：`update-character` 拒收带标题行的正文；`propose-design` 与 `append-design` 拒收给角色卡的 `##`（`cardHeadingError`）。开放长尾正是从这两个口子加的，不拦就是半截防线。
- `isFiller` 从 `report.ts` 挪到 `markdown.ts`（`report → characters` 已有依赖，反向 import 会成环），`report.ts` re-export 保住调用点。
- 顺带修了 `markdown.replaceSection` 的排版缺陷：改完的小节与下一节标题之间原本只有 `\n`，会黏在一起（整卡重建看不出来，外科改之后每次都出现）。现在与 `removeSection` 同一套拼法，块间恒为一个空行。

**同时修掉一个静默丢数据的缺陷**：`update-character` 原走 `parseCardBody → buildCardMarkdown` 整卡重建，任何规范外的小节（世界特有的「回响」、经 `append-design` 加的块）会被无声抹掉，而 confirm 文案还写着"其余保留"——那句话是假的。现在有回归用例钉住它。

**顺带修掉的既有故障**：`src/probe/advance.ts` 的替换字面量按 `\n` 写，而本机 `core.autocrlf=true` 使工作区是 CRLF——`bun run probe --check` 在第一处就抛。`mustReplace` 现在容忍两种行尾。

### P2 · 连续性文档 —— ⛔ 未实现

- `design/state/continuity.md` 走**开放文档**（`propose-design` / `apply-design` 的 `name` 分支，两段式已支持任意路径 + 小节级编辑 + `base` 快照防并发）。
- **不要做成第 5 个 `LayerId`**——会连带 `LayerId` 联合、`DESIGN_SPECS` 穷尽、`report.ts` 四层卡片、`resolveDoc` 的 `MAIN_LAYERS`、`design-spec` 白名单。按 `wiki/<题>.md` 的先例走 `name` 即可。
- 必须同时改 `src/framework/anchor.ts` 里 `buildDesignIndex` 的硬编码分组顺序 `["", "wiki", "characters", "outline"]`——不加 `"state"`，它在 `list-designs` 里**根本不出现**。
- 首次成稿必须是**整篇提案**：小节级提案要求文档与小节都已存在。

### P3 · 章末回写 —— ⛔ 未实现

- 触发点：**契约要挂在 `task` 的结果上**（仅 `agent === "writer"`）。章节在 writer 子会话里落盘，`save-chapter` 的结果**到不了 editor**；唯一到得了的是 `task` 的 `<task_result>`。
- 本性是**软纪律**，与"写章前先有已批准细纲"同类（`08 §6`），不做 harness 硬门禁。
- 写盘仍走两段式：卡的「当前」用 `update-character` 之外的专用入口，continuity 用 `propose-design` → `apply-design`。

---

## 8. 未决项

1. **常驻带没有载体**：今天没有任何代码承载"每场必带"，全靠 editor 手工切片。是否要一个 `character-brief`（只返回常驻带 + 「当前」）——这是"常驻 vs 按需"从约定变成机制的唯一办法。
2. **章末回写是软纪律还是硬门禁**（倾向软，见 §7 P3）。
3. **`当前` 的写入入口**：它被排除在 `update-character` 之外（防普通更新踩掉），现在的入口是 `propose-design(name, section:"当前")` 两段式——设计工具本就与路径无关，实测可用（`framework.test.ts` 有用例）。P3 的回写大概率直接复用这条，不必新造工具。
4. **世界特有的格是否需要一层"声明"**：目前自定义 `###` 完全自由，`design-spec(characters)` 只列了规范格；跨项目复用（如"系统流"的固定格）暂无机制。
5. **两条落卡入口的骨架保证不一致**（实测发现，2026-09-18）：真实会话里模型**没走** `add-character`（那张卡 12 小节、每节数百字，塞进结构化入参很别扭），而是走 `propose-design`+`apply-design`。后果有二：这张卡**没有 `### 当前`**（`add-character` 会恒定建出来，propose 不会），以及当时没有名单（已由"名单现算"解决）。**待定**：是否要给角色卡的整篇提案也补一道"骨架必须齐"的校验，还是接受"propose 路径可以不铺全骨架"。
6. **「待定」的写法还没有硬约束**：真实草稿里出现过 `（内容（待定））` 这种括号套括号（`world.md` 的术语表），工具认不出来，那一格会被当成"填了"。目前只认 `（待定…）` 与 `键：（待定…）` 两种。
