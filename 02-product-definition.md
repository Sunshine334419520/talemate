# 产品定义：AI 小说创作 Agent（网文写手工具）

> 日期：2026-08-30
> 状态：决策已锁定，待确认
> 关联文档：`01-feasibility.md` 是可行性探索，本文档是在其上做完所有关键决策后的**产品定义定稿**
> 一句话定位：**一个让"有想法的人 + 普通人"写出优秀网文的写手工具——把"不可言说的写作质量"变成可检查、可循环、可评测的标准。**

---

## 1. 项目性质与商业模式

| 项 | 决策 |
|---|---|
| 项目性质 | 商业产品为主（60%），开源倾向（30%），探索学习（10%） |
| 商业模式 | 前期用户**自定义 API key**；产品验证价值后提供 **token 订阅制** |
| 产品形态 | **完全独立的软件**，借鉴 OpenCode / Claude Code 的实现架构（不是依赖、不是插件生态） |

## 2. 目标用户

- **主用户：写手工具**——两类人：
  - **有想法的人**：有故事想法但写不好、写得慢
  - **普通人**：想写出优秀小说但不知从何下手
- **MVP 阶段的真实用户画像更窄**：愿意开终端、自带 API key 的写作者/极客。"普通人"随 UI 阶段进入。

## 3. 核心命题（护城河）

**成败押在一件事上：把"不可言说的写作质量"变成可检查的标准。** 文风是把标准定义出来，评判是按标准打分，循环是按标准收敛，评测是验证标准有提升。

网文行业验证了这个命题：起点编辑公开课反复讲，网文只有两根柱子——**代入感**（看得进开头）和**期待感**（想看后面）。这套工艺知识在行业里早就被编码成可检查的判据（爽点四段式、黄金三章、伏笔回收规则……），我们要做的是把它做成产品：**评判系统 + 编辑循环 + 评测基准**，五系统一主轴的"标准工程"。

## 4. 产品主流程：以"活的想法"为中心的反馈系统

小说写作的第一步是和用户一起**确定想法**，细化之后才能规划怎么写，然后才进入正文写作。**但这不是一次性的线性流水线——想法是"活的锚点"：初始想法可能只有 50% 完善度，写作依赖它，而写作过程又会浮现新点子，反过来修正想法。**

```
        ┌────── 写作中浮现的新点子 ──────┐
        ▼                              │
   想法工坊（活的，随时可改、可探讨）◄─────┤
        │                              │
        ▼                              │
     规划（随想法更新）──────────────────┤
        │                              │
        ▼                              │
     写作（每次都对着"当前想法"写）──────┘
```

- **想法工坊是常驻能力，不是第一阶段**：任何时候都能回到想法工坊，和 agent 讨论、改动企划书
- **写作依赖想法**：每次写作都引用当前版本的企划书 + 设定册
- **想法生长于写作**：写作浮现的新点子经反馈通道回到企划书

**用户角色贯穿始终**：想法阶段，用户是故事的"主人"（想法是他的，agent 是共创参谋）；写作阶段，用户是"主编"（拍板），agent 团队执行 + 批评。角色一直是用户，姿态从"出想法"变成"做裁决"。

### 新点子进企划书的两条通道（Q15）

- **用户手动**：任何时候回想法工坊，主动提出改动，agent 负责落进文档
- **agent 提议**：写作/批评中主动发现"值得升级的点子"提给用户（批评者兼任点子捕捉器）
- 两条通道都进同一个企划书，**用户拍板**

### 想法变更的影响范围（Q16）

- **只影响后续**：已写章节不动，从下一章起按新设定写
- "回头核查已写章节的一致性"归长篇状态层，后置

### 各阶段工件

| 阶段 | 产出物 | 内容 |
|---|---|---|
| **想法工坊** | **企划书（活文档）** | 一句话卖点 + 频道/题材 + 主角卡 + 金手指 + 核心冲突 + 爽点定位 + 目标读者。初始 ~50% 完善度，随写作持续生长 |
| **规划** | **迷你设定册** | 全书一句话主线 + 主线悬念 + 中期目标 + 前 3 章细纲 + 主角/关键配角卡 + 金手指规则 |
| **写作** | **章稿 v0/v1 + 批评者报告** | 判据 ✅/❌ + 证据引用（原文）+ 改法（按优先级排序） |

> **关键区分**：企划书 + 设定册是**创作设定的活文档**——从想法阶段开始积累并持续更新；**状态层**（连续性日志、伏笔登记、跨章追踪、跨会话记忆）是长篇独有，后置。
>
> **想法变更记录（版本感）**：企划书的每次改动留 v1→v2 差异可见，避免"随时改"让人迷失。这是版本感，不是状态层。

## 5. 标准体系：双档制（网文包）

**体裁包机制**：标准框架"机制通用 + 判据可插拔"，网文是第一个体裁包；网文包内部按**频道**（男频/女频）调整判据权重。

**质量锚点采用双档制**（批评者先抓硬伤，再逼优秀）：

### 合格线（9 维硬判据）——及格才有资格谈好坏

| # | 维度 | 核心可检查判据 |
|---|---|---|
| 1 | **爽点节奏/密度** | 每 ~2000 字至少 1 个落地爽点；**爽点四段式**完整（铺垫轻蔑 → 反差展示 → 当面反馈具名 → 主角回应）；相邻爽点不同类；胜利必须被利益相关者看见 |
| 2 | **期待管理** | 兑现对应律（满足的期待与制造的期待必须对应）；"放鸽子/假高潮"检测；微/中/大爽分层与铺垫量匹配；破局"意料之外、情理之中"，不机械降神 |
| 3 | **黄金三章** | 第1章前 300 字从冲突切入、500 字内金手指落地、主角开局像普通人；第2章当面打脸 + 必须升级；第3章抛出中期目标 + 强敌 + 非做不可的理由 |
| 4 | **情绪曲线** | 单章"蓄力→释放→余波"完整弧线；上拐点与下拐点感受截然相反；三种不良曲线扣分（长平台/连续下滑/密集尖峰） |
| 5 | **章末钩子** | 最后三行制造"未完成心理缺口"（决定/发现/误判/代价四种断法选一）；钩子类型须轮换；在情绪最高点断；上一章钩子在本章前 30% 回收 |
| 6 | **人物立体度** | 工具人检测（捧哏同伴/恶毒反派/无知群众/嫉妒姐妹四型）；反派按主角写（有立场、有超预期行为、弱点是人性而非犯蠢）；配角智商前后一致 |
| 7 | **伏笔回收** | 埋设必须有表面功能；回收五条件可逐条核查；"其实我早就……"无前迹 = 不合格回收 |
| 8 | **信息密度** | 节奏本质是"单位字数内的有效信息量"；删掉一个事件剧情是否受影响（不受影响 = 流水账/注水）；对话每句有信息量；数据不需读者现场换算 |
| 9 | **失败模式检测** | 脑残剧情 / 套路化 / 注水 / 逻辑硬伤 / 金手指滥用 / 频道红线（男频：舔狗绿帽；女频：雌竞无脑原谅）/ 微毒点 |

### 优秀区（6 条软判据）——从"合格"到"优秀"的分水岭

1. **是否尊重读者智商**——有没有角色"为了让主角赢而变蠢"？
2. **破局是否意料之外、情理之中**——关键转折是否依赖回收章首次出现的新规则/新证据（机械降神）？
3. **人物是否可预测**——配角有自己的利益、会说谎、会拒绝、有独立故事线，而非一开口就知道是功能牌
4. **情绪兑现 vs 事件兑现**——读者真正得到的是"憋屈→释放"的情绪兑现，而不只是"事件发生了"
5. **群像与主题**——配角有自己的高光与弧光，有现实隐喻（十日终焉"六不" + 40+ 人物小传）
6. **需要动脑**——"突然需要动脑去看"是优秀网文的可测量信号；平庸文"闭眼也能看"

> 平庸 = 9 维及格但全可预测；优秀 = 及格线之上 + 不降智、不工具人、有认知反转、有情绪兑现。

**男频/女频关键差异（评分时按频道调权重）**：男频爽点密度 1-2 章一爆点、先装逼；女频 3-4 章一情绪高潮、先亲嘴、虐完必补偿、讲究体面不掉价；女频有标志性"情绪挤压"手法。感情线：女频四段式先慢后快，男频感情是调味剂。平台差异：起点可 10 章铺垫，番茄 3 秒划走、3 章一爽。

## 6. 评判系统

**绝不打一个总分。** 批评者输出**四件套**：

```
判据 ✅/❌ → 证据（引用原文指出问题） → 改法（按优先级：先结构 → 场景 → 句子，不能一次全改） → 一致性提示（本章设定与企划书的出入 / 值得升级进企划书的新点子）
```

分数只是副产品，用于收敛判断。**写手与批评者必须分离，批评者是独立上下文**——同一上下文自我批评会 bias；批评者只拿稿子 + 判据说话。批评者兼任"点子捕捉器"（见 §4 反馈通道）。

## 7. 编辑循环：用户当主编

循环能控制质量，前提是：标准可检查（双档制） + 写手批评者分离 + 有停止条件。

**停止条件**：① 分数达标（合格线全过、优秀区有亮点）；② 无改善停（连续两轮分不涨就停）；③ 轮数上限（单章 2-3 轮封顶）。

**对话式 TUI 交互**：用户打字下指令（"背叛不够狠，加一个"），agent 团队起草 → 自评 → 改稿，终端给用户看 v0/v1 差异 + 批评者报告，循环到用户拍板。

**循环对着"当前企划书"跑**：每次起草都引用当前版本的企划书 + 设定册；批评者的"一致性提示"若被用户采纳，写手按新设定重写，企划书相应更新。

**写手的推进方式调色盘**（写手和批评者共用的专业知识，沿用自 01 文档）：场景/概括/对话驱动/内心驱动/行动驱动/悬念/反转/意象——写手在每段显式声明"这段用什么推进方式"，批评者检查选得对不对、用得纯不纯。

## 8. 评测基准（元系统，开发工具不是产品功能）

必须有——它是"调教"的仪表盘。四个固定：

1. **固定测试集**：20 个固定用例按网文类型重配——黄金三章、爽点章、钩子章、人设场景、转折章、单章完整体
2. **固定评分协议**：独立评分 agent + 固定标准（双档制判据）
3. **基线对比**：改之前记基线，改之后跑同一套，升了才算数，降了回滚
4. **盲评 + 并排比较**：评分者不知道哪篇是旧版哪篇是新版，主指标用相对比较而非绝对分数

**产品和开发共用同一套标准**：产品里批评者打分驱动循环；开发时同一套标准跑基准驱动调教。

## 9. 架构：借 OpenCode 的边界

**整体形状**：无头 server 核心 + 薄客户端（TUI 先行，Web/桌面后置）+ 类型化 SDK。

### 借（高价值复用）
- **agent 循环**（`session/prompt.ts` 的 `runLoop`）
- **子代理机制 = task 工具创建子 Session**——**这正是"写手/批评者分离 + 批评者独立上下文"的现成实现**。导演会话在主线，写手/批评者各开子会话，互不污染
- **上下文管理**（溢出检测 + 压缩 + 摘要），provider 无关，直接搬
- **Skill 发现机制**（SKILL.md 自动注入 system prompt）——映射文风包 / 题材包 / 世界观设定册
- **Tool 框架 + 插件钩子**（`ToolDefinition` 自定义工具，不动核心）——"写章节 / 评章节 / 改章节"挂这里
- **Provider 抽象**——自定义 API key 直接对上

### 剥（不匹配，去掉）
- 本地文件 / git / worktree / pty / LSP 绑定
- 面向 CLI 审批的 permission / ask 模型（写作工具重新想权限模型）

## 10. MVP 范围：浅-浅-深

第一版端到端切片：

| 阶段 | 深度 | 验证点 |
|---|---|---|
| 想法工坊 | **浅**：走一遍真实的想法细化对话，建立 ~50% 的企划书（活文档起点） | 用户 30 分钟内从"一个想法"到企划书 |
| 规划 | **浅**：产出迷你设定册 + 前 3 章细纲 | 黄金三章判据有据可评 |
| 第一章写作 | **深**：单章编辑循环 + 双档批评者 | **第一章能否从合格线推到优秀区** |

> MVP 成败指标 = 第一章质量。idea→plan 薄薄走通当"输入"，第一章循环当"验证点"。
>
> MVP 也要演示一次**反馈闭环**：写作第一章中浮现一个新点子 → 回写企划书 → 用户看到 v1→v2 变化。这是"活的锚点"的最小证明。

## 11. 后续演进（分期）

1. **UI 阶段**：Web/桌面工作台（章为单位：大纲、人设、章节、循环进度、审阅按钮），普通人进入
2. **上传提取文风**：风格转写机制（3-5 篇样本 → 提取 agent → 结构化风格规范），第二阶段差异化
3. **状态层 / 长篇**：连续性日志、伏笔登记、跨会话记忆、全卷大纲——换作品结构 + 加状态层，写作引擎不动
4. **token 订阅制**：产品验证价值后推出

## 12. 决策记录（Q1-Q14）

| # | 决策点 | 决策 | 关键理由 |
|---|---|---|---|
| Q1 | 项目性质 | 商业 60 / 开源 30 / 探索 10 | 标准工程论点只有被真实使用才成立 |
| Q2 | 目标用户 | 写手工具（有想法的人 + 普通人） | 质量要求最真实、能容纳用户当主编 |
| Q3 | 体裁 | 网文向优先，体裁可选 | 网文内部也有垃圾/优秀之分，标准工程依然成立 |
| Q4 | 人在循环 | 用户当主编 | 保住质量天花板 + "这是**我**写的小说"身份感 |
| Q5 | 框架 | 独立软件，借 OpenCode/Claude Code 架构 | 非 DeepSeek Harness（太激进），形态自由度最高 |
| Q6 | 标准体系 | 体裁包机制，网文包先行 | 标准框架本身成为可积累的产品资产 |
| Q7 | 工作单元 | 单章先行 | 网文是长篇连载，章是产品单元，编辑循环以章为单位 |
| Q8 | 产品形态 | CLI 先行，UI 后置 | 写不出好文章，UI 无用 |
| Q9 | 商业 | 前期自定义 API key，后期订阅 | 编辑循环 token 成本是定价前提 |
| Q10 | 质量锚点 | 双档制（合格线 + 优秀区） | 合格判据易写立刻有用，优秀判据难写需样本反推 |
| Q11 | 第一版起点 | **修正为全流程**：想法→规划→写作 | 小说第一步是和用户确定想法，不是直接写第一章 |
| Q12 | CLI 交互 | 对话式 TUI | 用户当主编的本质是一场对话 |
| Q13 | MVP 切片 | 浅-浅-深 | 成败押在第一章质量，idea→plan 是输入 |
| Q14 | 工件 | 企划书 / 迷你设定册 / 章稿+批评者报告 | 静态设定从想法阶段积累，状态层仍后置 |
| Q15 | 新点子进入机制 | 两条通道（用户手动 + agent 提议），用户拍板 | 用户是主人，agent 是参谋；批评者兼任点子捕捉器 |
| Q16 | 想法变更影响范围 | 只影响后续；已写章节一致性核查归长篇状态层 | 版本感（v1→v2 差异）要有，状态层后置 |

---

## 附：网文判据资料来源

- [起点编辑星河/青狐公开课（代入感、期待感、爽点密集）](https://www.snm0516.aisee.tv/video/BV1ryZaYLEkz/)
- [商业网文模式手册（爽点四段式、黄金三章清单、卷级冲突升级）](https://github.com/Yunshiro/yunn-skills/blob/main/skills/novel-writer/references/commercial-mode.md)
- [网文伏笔与回收（webnovel-handbook）](https://github.com/miserylee/webnovel-handbook/blob/main/docs/storycraft/63-foreshadowing-callback-reader-memory-management.md)
- [黄金三章检查指南（马良写作）](https://maliangwriter.com/blog/golden-three-chapters-check-guide/)
- [网文节奏与爽点设计（马良写作）](https://maliangwriter.com/blog/novel-pacing-hook-design-guide/)
- [男频与女频写作手法区别（知乎）](https://www.zhihu.com/question/412007500)
- [《十日终焉》评价（中国作家网圆桌研讨）](https://www.chinawriter.com.cn/n1/2026/0522/c404027-40725150.html)
- [网文期待感：兑现的期待与制造的期待](https://m.toutiao.com/w/1820047508846732/)
- [网文毒点避坑指南（wangwen666）](https://wangwen666.com/post/21.html)
