# 产品定义：AI 小说创作 Agent（网文写手工具）

> 日期：2026-08-30 · **2026-09-04 重大修订**（见下）
> 状态：2026-09-04 起以修订版为准
> 关联文档：`01-feasibility.md`（可行性探索）+ `04-harness-design.md`（当前 harness 基准）

> ## ⚠️ 2026-09-04 重大修订：移除评判系统，转向"项目空间 + 创作 Agent"
>
> 产品主线发生根本变化，本文件旧正文中与之冲突的部分**作废**：
>
> | 旧论点（作废） | 新方向（现行） |
> |---|---|
> | 护城河 = "把质量变成可检查的标准"（双档判据 + 批评者 + 编辑循环收敛） | 护城河 = **创作过程系统化**：设计段把企划做厚、写作段结构先行（规划→节拍）+ 用户当主编拍板 |
> | 质量靠事后打分循环（评判系统）收敛 | 质量靠**写前把结构和设定做对** + 独立上下文分工 + 人拍板 |
> | 批评者兼任点子捕捉器、自动回写企划书 | 回写通道暂不做：企划改动由主编对话 + 用户拍板落盘 |
> | 五系统一主轴（评判系统是主轴） | 评判系统整体移除；产品 = **项目空间承载的多角色创作 Agent** |
>
> - **下文 §5「双档制」、§6「评判系统」、§7「编辑循环」及其决策记录视为历史参考，不再是产品功能。** 其中可复用的技法知识（爽点四段式、节拍、文风维度等）以"写作知识"形态后续沉淀进 skill 库，不作评分判据。
> - **§4 主流程（想法工坊→规划→写作、活的锚点）依然有效**，是产品主线；它由 harness 承载，实现见 `04-harness-design.md`（P0 已完成，P1 接入写作领域）。
> - 产品结构从"标准工程"改为：**项目（一部小说一个空间）→ 设计段（主编 + 用户，企划分层活文档）→ 写作段（用户手动触发，task 委派 规划/写手）→ 用户主编拍板**。无自动评判循环。
>
> 一句话定位（修订）：**一个让"有想法的人"把小说从念头写到成稿的创作 Agent——以"活的小说项目空间"为中心，设计段帮你想清楚、写作段帮你写出来，全程你当主编。**

---

---

## 1. 项目性质与商业模式

| 项 | 决策 |
|---|---|
| 项目性质 | 商业产品为主（60%），开源倾向（30%），探索学习（10%） |
| 商业模式 | 前期用户**自定义 API key**；产品验证价值后提供 **token 订阅制** |
| 产品形态 | **完全独立的软件**，借鉴 OpenCode / Claude Code 的实现架构（不是依赖、不是插件生态） |

## 2. 目标用户

- **主用户：写手工具**——两类人：
  - **有想法的人**：有故事想法但写不好、写得慢
  - **普通人**：想写出优秀小说但不知从何下手
- **MVP 阶段的真实用户画像更窄**：愿意开终端、自带 API key 的写作者/极客。"普通人"随 UI 阶段进入。

## 3. 核心命题（护城河）

**成败押在一件事上：能不能让 agent 写出"作者愿意认领"的章节——把写小说这件高门槛的事，拆成"想清楚 → 写出来 → 人拍板"可协同的流程。**（2026-09-04 修订：不再以"事后打分收敛"为护城河。）

网文行业验证了这套工艺知识：起点编辑公开课讲代入感/期待感两根柱子，行业里有爽点节奏、节拍规划、伏笔回收等成熟做法。talemate 的做法是**把它们前置到生成时**：设计段用对话把设定做厚，写作段用"规划（节拍）→ 写手"的结构让章节在写之前就有读者体验设计；质量靠**上游结构 + 独立上下文分工 + 用户主编拍板**，而非事后批评循环。

## 4. 产品主流程：以"活的想法"为中心的反馈系统

小说写作的第一步是和用户一起**确定想法**，细化之后才能规划怎么写，然后才进入正文写作。**但这不是一次性的线性流水线——想法是"活的锚点"：初始想法可能只有 50% 完善度，写作依赖它，而写作过程又会浮现新点子，反过来修正想法。**

```
        ┌────── 写作中浮现的新点子 ──────┐
        ▼                              │
   想法工坊（活的，随时可改、可探讨）◄─────┤
        │                              │
        ▼                              │
     规划（随想法更新）──────────────────┤
        │                              │
        ▼                              │
     写作（每次都对着"当前想法"写）──────┘
```

- **想法工坊是常驻能力，不是第一阶段**：任何时候都能回到想法工坊，和 agent 讨论、改动企划书
- **写作依赖想法**：每次写作都引用当前版本的企划书 + 设定册（docs/ 分层活文档，绝不缓存旧设定）
- **想法生长于写作**：写作中浮现的新点子，由主编（或用户）在对话中提出并落回企划书。**自动"点子捕捉器/回写通道"暂不做**（2026-09-04 决策），先由人驱动。

**用户角色贯穿始终**：设计段，用户是故事的"主人"（想法是他的，主编 agent 是共创参谋）；写作段，用户是"主编"（拍板），agent 是执行者。角色一直是用户，姿态从"出想法"变成"做裁决"。

### 企划变更通道（2026-09-04 修订，替代旧 Q15 两条通道）

- **用户手动为主**：任何时候回主编会话，主动提出改动，editor 负责读当前版本、落进对应 docs 文档（覆盖前确认）。
- **editor 提议为辅**：对话中 editor 发现"值得升级的点子/前后矛盾"提给用户，用户拍板后才落盘。
- 变更都进同一份企划书（docs/），**用户拍板**。版本感靠项目进 git 的 diff，不做应用层版本历史。

### 想法变更的影响范围（Q16，沿用）

- **只影响后续**：已写章节不动，从下一章起按新设定写
- "回头核查已写章节的一致性"归长篇状态层，后置

### 各阶段工件（修订）

| 阶段 | 产出物 | 内容 |
|---|---|---|
| **设计段（主编对话）** | **企划书 = 分层活文档（docs/）** | core.md（卖点/设定）+ world.md（世界观）+ characters.md（角色）+ outline.md（大纲）。初始 ~50% 完善度，持续迭代 |
| **写作段（用户手动触发）** | **章节规划 + 正文章稿** | 用户说"写第 N 章" → task 委派 规划（体验工程图/节拍）→ 写手（正文）→ 成品落 chapters/ |
| **用户拍板** | 落盘即确认 | 覆盖设定 / 落盘成品前 confirm 给用户看影响面 |

> **关键区分**：企划书（docs/）是**创作设定的活文档**——从设计段开始积累并持续更新；**状态层**（连续性日志、伏笔登记、跨章追踪、跨会话记忆）是长篇独有，后置。

## 5-7. 评判系统 / 双档判据 / 编辑循环 —— 已移除（历史参考）

> 2026-09-04 决策：**这三者不再是产品功能。** 原 §5 双档制（合格线 9 维 + 优秀区 6 条）、§6 评判系统（批评者四件套）、§7 编辑循环（自评→改稿循环）整体移除。
>
> - 移除理由：事后打分循环收敛质量，成本高、收敛慢，且让产品押在"评判可检查"这一不可靠前提上。改为**质量前置**——设计段把设定做厚、写作段结构先行（规划切节拍再写正文）+ 用户主编拍板。
> - 原判据里**可复用的写作技法**（爽点四段式 / 黄金三章 / 章末钩子四断法 / 伏笔回收规则 / 男频女频节奏差异等）不丢：后续沉淀成 **skill 库的技法包**，写作/规划时作为知识注入（见 `04-harness-design.md` §6），**不作为评分判据**。
> - 原 §7 的"写手推进方式调色盘"同属技法知识，随技法包沉淀。
> - 一致性：editor 对话中发现设定出入，直接提给用户拍板（§4 企划变更通道），不设独立"一致性提示"环节。

## 8. 开发期评测（开发工具，不是产品功能；方向保留）

开发期仍需"改了有没有变好"的仪表盘，但**不再依赖打分判据**——因为质量信号改成"作者是否认领成品 / 章节是否贴近设定与规划"。方向：跑真实用例、由作者（你）对比章节与规划/设定的贴合度做人工基线；等积累后再决定是否加半自动对比。这块 P2 之后再细化。

## 9. 架构：借 OpenCode 的边界

**整体形状**：无头 server 核心 + 薄客户端（TUI 先行，Web/桌面后置）+ 类型化 SDK。

### 借（高价值复用）
- **agent 循环**（`session/prompt.ts` 的 `runLoop`）
- **子代理机制 = task 工具创建子 Session**——**这正是"写手/批评者分离 + 批评者独立上下文"的现成实现**。导演会话在主线，写手/批评者各开子会话，互不污染
- **上下文管理**（溢出检测 + 压缩 + 摘要），provider 无关，直接搬
- **Skill 发现机制**（SKILL.md 自动注入 system prompt）——映射文风包 / 题材包 / 世界观设定册
- **Tool 框架 + 插件钩子**（`ToolDefinition` 自定义工具，不动核心）——"写章节 / 评章节 / 改章节"挂这里
- **Provider 抽象**——自定义 API key 直接对上

### 剥（不匹配，去掉）
- 本地文件 / git / worktree / pty / LSP 绑定
- 面向 CLI 审批的 permission / ask 模型（写作工具重新想权限模型）

## 10. MVP 范围：浅-浅-深

第一版端到端切片：

| 阶段 | 深度 | 验证点 |
|---|---|---|
| 想法工坊 | **浅**：走一遍真实的想法细化对话，建立 ~50% 的企划书（活文档起点） | 用户 30 分钟内从"一个想法"到企划书 |
| 规划 | **浅**：产出迷你设定册 + 前 3 章细纲 | 黄金三章判据有据可评 |
| 第一章写作 | **深**：单章编辑循环 + 双档批评者 | **第一章能否从合格线推到优秀区** |

> MVP 成败指标 = 第一章质量。idea→plan 薄薄走通当"输入"，第一章循环当"验证点"。
>
> MVP 也要演示一次**反馈闭环**：写作第一章中浮现一个新点子 → 回写企划书 → 用户看到 v1→v2 变化。这是"活的锚点"的最小证明。

## 11. 后续演进（分期）

1. **UI 阶段**：Web/桌面工作台（章为单位：大纲、人设、章节、循环进度、审阅按钮），普通人进入
2. **上传提取文风**：风格转写机制（3-5 篇样本 → 提取 agent → 结构化风格规范），第二阶段差异化
3. **状态层 / 长篇**：连续性日志、伏笔登记、跨会话记忆、全卷大纲——换作品结构 + 加状态层，写作引擎不动
4. **token 订阅制**：产品验证价值后推出

## 12. 决策记录（Q1-Q16）

> 2026-09-04 修订：以下各 Q 中凡以"评判/编辑循环/标准工程"为理由或把批评者当产品的，均已按 §0 重大修订更新；标注 **（旧·已修订）** 的行请以修订理由为准。Q1-Q5/Q8/Q11/Q12/Q16 决策本身依然有效。

| # | 决策点 | 决策 | 关键理由 |
|---|---|---|---|
| Q1 | 项目性质 | 商业 60 / 开源 30 / 探索 10 | 创作工具论点只有被真实使用才成立 |
| Q2 | 目标用户 | 写手工具（有想法的人 + 普通人） | 质量要求最真实、能容纳用户当主编 |
| Q3 | 体裁 | 网文向优先，体裁可选 | 网文内部也有好/差之分，创作流程系统化依然成立 |
| Q4 | 人在循环 | 用户当主编 | 保住质量天花板 + "这是**我**写的小说"身份感 |
| Q5 | 框架 | 独立软件，借 OpenCode/Claude Code 架构 | 非 DeepSeek Harness（太激进），形态自由度最高 |
| Q6 | 标准体系 | **（旧·已修订）体裁包/双档判据 → 改为技法知识包** | 评判判据不再做产品；可复用技法沉淀为 skill 技法包 |
| Q7 | 工作单元 | 单章先行 | 网文是长篇连载，章是产品单元，写作用户手动逐章触发 |
| Q8 | 产品形态 | CLI 先行，UI 后置 | 写不出好文章，UI 无用 |
| Q9 | 商业 | 前期自定义 API key，后期订阅 | agent 调用 token 成本是定价前提 |
| Q10 | 质量锚点 | **（旧·已修订）双档制 → 无** | 不再打分收敛；质量靠上游结构 + 人拍板 |
| Q11 | 第一版起点 | **修正为全流程**：设计（想法→企划）→ 写作（用户手动触发） | 小说第一步是和用户确定想法，不是直接写第一章 |
| Q12 | CLI 交互 | 对话式 TUI | 用户当主编的本质是一场对话 |
| Q13 | MVP 切片 | **（旧·已修订）浅-浅-深押第一章 → P0 harness + P1 领域接入** | 先立骨架再填领域（见 04 §12 里程碑） |
| Q14 | 工件 | **（旧·已修订）章稿+批评者报告 → 章稿 + 规划落盘** | 见 §4 修订表：设计段分层活文档 / 写作段 规划+正文 |
| Q15 | 新点子进入机制 | **（旧·已修订）两条通道 → 用户手动为主 + editor 提议** | 自动点子捕捉/回写通道暂不做；人驱动 |
| Q16 | 想法变更影响范围 | 只影响后续；已写章节一致性核查归长篇状态层 | 版本感（diff 可见）要有，状态层后置 |

---

## 附：网文判据资料来源

- [起点编辑星河/青狐公开课（代入感、期待感、爽点密集）](https://www.snm0516.aisee.tv/video/BV1ryZaYLEkz/)
- [商业网文模式手册（爽点四段式、黄金三章清单、卷级冲突升级）](https://github.com/Yunshiro/yunn-skills/blob/main/skills/novel-writer/references/commercial-mode.md)
- [网文伏笔与回收（webnovel-handbook）](https://github.com/miserylee/webnovel-handbook/blob/main/docs/storycraft/63-foreshadowing-callback-reader-memory-management.md)
- [黄金三章检查指南（马良写作）](https://maliangwriter.com/blog/golden-three-chapters-check-guide/)
- [网文节奏与爽点设计（马良写作）](https://maliangwriter.com/blog/novel-pacing-hook-design-guide/)
- [男频与女频写作手法区别（知乎）](https://www.zhihu.com/question/412007500)
- [《十日终焉》评价（中国作家网圆桌研讨）](https://www.chinawriter.com.cn/n1/2026/0522/c404027-40725150.html)
- [网文期待感：兑现的期待与制造的期待](https://m.toutiao.com/w/1820047508846732/)
- [网文毒点避坑指南（wangwen666）](https://wangwen666.com/post/21.html)
