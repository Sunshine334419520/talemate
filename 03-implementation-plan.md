# 实现计划：AI 小说创作 Agent

> 日期：2026-08-30 · **2026-09-04 重大修订**
> 上游：`01-feasibility.md`（可行性）+ `02-product-definition.md`（产品定义定稿，2026-09-04 已修订去评判）+ `04-harness-design.md`（当前实现基准）

> ## ⚠️ 2026-09-04 重大修订：从"判据循环验证"转向"自建 harness 基础设施"
>
> - **§5「P1a 单章循环验证（批评者循环）」整体作废**——那是评判系统的验证实验，评判系统已从产品移除（见 02 §0）。
> - 新的实现路径在 **`04-harness-design.md`**：P0 自建 harness 基础设施（core/storage/llm/agent/session/tool/skill/context，**已完成并冒烟通过**）→ P1 接入写作领域 → P2 产品化。
> - 本文档 §2 技术栈（TS + Bun、无 Effect、文件系统存储先行）、§3 架构借 opencode 边界、§4 领域模型方向仍然有效，具体以 04 落地为准。§1 的"在自建架构上验证、不靠外部 harness"已变为现实（自建 harness 即产品内核，非仅为验证）。

---

## 1. 总体策略：在自建架构上验证，不靠外部 harness

**核心命题是"标准工程"**（Q3/Q6/Q10 锁定的双档判据 + 编辑循环）。标准不成立，架构全是空转——这和你 Q8"写不出好文章，UI 无用"是同一个逻辑，放大到整个产品层面。

**决策：跳过抛掷实验（P0），直接建产品骨架，在自建架构上验证。** 理由：现有 harness 不足以承载这个实验（这正是自建的初衷）；而且 P1 的第一个里程碑本身就是"单章编辑循环"——验证胜负手的动作内嵌在 P1 里，不额外花时间。

```
P1 产品骨架（独立代码库）
  ├─ P1a 核心引擎 + 单章循环验证（胜负手）
  │    门禁：3 用例 v0→v2 上升曲线；≥2/3 进入优秀区
  ├─ P1b 三阶段流程 + 活的想法 + 对话式 TUI
  │    门禁：用户 30 分钟从"一个想法"到"第一章 v1 + 批评者报告"
  ▼
P2 演进（Web UI / 提取文风 / 长篇状态层 / 订阅制）
```

---

## 2. 技术栈决策 ⚠️（需确认）

| 项 | 推荐 | 理由 |
|---|---|---|
| 语言 | **TypeScript** | 镜像 OpenCode（参考架构同语言，借思路最顺） |
| 运行时 | **Bun 或 Node**（见下） | 两者都满足本架构；差别在开发体验与熟悉度 |
| 框架风格 | **不用 Effect** | OpenCode 用 Effect（函数式），学习成本高、招人难、对独立产品是负担。用清晰的模块边界 + 直接代码 |
| 参考代码 | OpenCode `~/code/opencode/packages/` | 只读参考其架构模式，不引入其代码（Q5：独立软件） |

### Bun vs Node 怎么选

**Bun**：一个 JS/TS 运行时（Node 的现代替代品），作者也是前端打包工具 esbuild 的作者。核心特点是 **all-in-one**——装一个二进制，自带包管理器（`bun install`）、TS 直接运行（`bun run file.ts`，不用先编译）、测试器（`bun test`）、打包器（`bun build`），零构建配置；启动和执行比 Node 快；内置 fetch / WebSocket / HTTP server API，正好对上"无头 server + SSE 流式"的架构。

**Node**：生态最成熟、有长期支持版本（LTS）、每个开发者都熟。现代 TS 工具链（tsx 运行、vitest 测试）已经把开发体验差距追得差不多。

**选型建议**：两者都能承载本架构，且选型**不影响架构设计**（§3 全是模式，与运行时无关）。
- 倾向 **Bun** 的理由：① OpenCode 是 Bun 原生，我们读它参考代码时工具链/习惯最顺；② 零构建配置，起步最快。
- 倾向 **Node** 的理由：生态最稳、你最熟、零风险。

> 结论：**默认 Bun，选 Node 也完全成立**——这是舒适度选择，不是技术胜负。Rust（Claude Code 同款）开发慢，不推荐做 MVP。

---

## 3. 架构：借 OpenCode 的边界（具体化）

### 3.1 整体形状（从 OpenCode 借的第一性模式）

**无头核心 + 薄客户端**。OpenCode 里 TUI 只是 HTTP 客户端，核心是 `opencode serve` 的无头 server——这正是"UI 换掉核心不动"的架构，也是你 CLI 先行、Web 后置的地基。

```
┌─────────────────────────────────────────────────┐
│           对话式 TUI（薄客户端，Phase 1）          │
│   想法工坊 │ 大纲 │ 章节工作台 │ v0/v1 diff │ 评分   │
└────────────────────────┬────────────────────────┘
                         │ HTTP + SSE（流式）
┌────────────────────────▼────────────────────────┐
│            无头核心（独立进程）                    │
│  ┌──────────────┐   ┌───────────────┐           │
│  │ 导演会话      │   │ 子代理工厂     │           │
│  │ (Agent Loop) │──▶│ 写手 / 批评者  │           │
│  └──────┬───────┘   └───────┬───────┘           │
│         │                   │ 各自独立上下文      │
│  ┌──────▼───────┐   ┌──────▼───────┐           │
│  │ Tool 框架     │   │ Skill / 判据  │           │
│  │ 写章/评章/改章 │   │ 双档标准包     │           │
│  └──────┬───────┘   └──────┬───────┘           │
│         └─────────┬────────┘                    │
│            ┌──────▼───────┐                     │
│            │ 会话 / 存储    │                     │
│            │ 企划书/设定册/ │                     │
│            │ 章稿/批评报告  │                     │
│            └──────────────┘                     │
└─────────────────────────────────────────────────┘
```

### 3.2 借什么（从 OpenCode 的架构模式）

| 借的架构模式 | OpenCode 参考实现 | 我们怎么用 |
|---|---|---|
| **Agent loop**（单 while(true)：取消息 → 构建模型消息 → 流式 LLM → 工具调用 → 追加 → 循环） | `src/session/prompt.ts` | 导演会话的骨架；写手/批评者走同一循环，只是不同 agent 定义 |
| **子代理 = 子会话独立上下文** | `src/tool/task.ts` + `src/agent/agent.ts` | **写手/批评者分离的机制**：各开子会话，批评者只拿稿子+判据，看不到写手思维 |
| **上下文管理**（溢出检测 + 压缩 + 摘要） | `src/session/compaction.ts` | 长会话/长篇前期的续写保障 |
| **Skill 注入 system prompt**（SKILL.md 自动发现） | `src/skill/index.ts` | 双档判据包、文风包、题材包、设定册都挂这里 |
| **Tool 框架**（`Tool.define` + 注册表） | `src/tool/tool.ts` + `registry.ts` | 写章/评章/改章/更新企划书等自定义工具 |
| **无头 server + 类型化客户端** | `src/server/server.ts` | 核心与 UI 的边界 |
| **会话持久化** | `packages/core/src/session/sql.ts` | 章稿/报告的落盘（我们用文件系统，见 §4） |

### 3.3 剥什么（OpenCode 里我们不需要的）

- 本地文件 / git / worktree / pty / LSP 工具——写作工具是文本世界
- 面向 CLI 审批的 permission / ask 模型——权限模型重新想（写作工具的"审批"是用户当主编的拍板，不是工具权限）
- Effect 依赖（若不采用该风格）

### 3.4 我们自己的新增（OpenCode 没有，产品独有）

1. **领域模型**：小说对象（企划书/设定册/章稿/批评报告/一致性）
2. **写手 / 批评者两个领域 agent**（带角色 prompt + 判据）
3. **双档判据**（作为数据/技能资产，不是硬编码——判据要能被评测基准迭代）
4. **v0/v1 diff 视图** + 批评者四件套报告
5. **想法变更记录**（企划书 v1→v2 差异可见，Q16 的版本感）

---

## 4. 领域模型（核心数据结构）

```
小说 Project
├── 企划书 IdeaBook（活文档，带版本历史）      ← 想法工坊产出，常驻可改
│     ├─ 一句话卖点 / 频道(男频/女频) / 题材
│     └─ 主角卡 / 金手指 / 核心冲突 / 爽点定位 / 目标读者
├── 设定册 SettingBook                        ← 规划产出
│     ├─ 全书一句话主线 / 主线悬念 / 中期目标
│     ├─ 前 3 章细纲 / 关键配角卡 / 金手指规则
│     └─ 每章定位（开篇/中段/高潮/收尾）
├── 章节 Chapter
│     ├─ 稿：v0 / v1 / v2 …
│     └─ 批评报告 Critique（四件套：判据✅/❌ → 证据 → 改法 → 一致性提示）
└── 一致性对账（章稿 ↔ 企划书/设定册的出入，批评者产出，用户拍板）
```

**存储：文件系统目录，一小说一目录**（`idea/` 带版本、`setting.md`、`chapters/01.md`、`critiques/01.md`）。原因：透明可 diff、可进 git、调试友好；等需要多用户/并发再上 SQLite。

---

## 5. 实现路径（2026-09-04 修订：取代原 P1a 判据循环）

原 §5「P1a 单章循环验证（写手→批评者→重写→再评 + 双档判据）」**作废**（评判系统已移除）。新实现以自建 harness 为主干，里程碑与详情见 **`04-harness-design.md` §12**。要点：

- **P0 · harness 基础设施**（已完成，冒烟通过）：`core`(类型/配置) · `storage`(文件系统项目+会话) · `llm`(anthropic/openai 兼容/mock 流式+工具循环) · `agent`(editor/planner/writer 注册表) · `skill`(SKILL.md 目录注入) · `tool`(defineTool+内置工具) · `session`(post→agent 循环→task 委派→compaction) · `cli.ts`(REPL)。
- **P1 · 写作领域接入**：把已有写作技法（`src/legacy/prompts.ts` 的 STRUCTURE/PROSE spec、文风卡双层格式）接入 writer/planner agent system；editor 设计对话驱动 docs/ 分层活文档。
- **领域资产迁移**：原 `experiments/` 用例 → 迁移进受管项目（`novel create` 建真实小说空间）；文风卡 → SKILL.md 技能库。
- 不作任何"评分/批评/改稿循环"——质量的落地点是 设定做厚 + 结构先行 + 用户拍板。

---

## 6. 里程碑（2026-09-04 修订）

| 阶段 | 内容 | 验证指标 | 门禁 |
|---|---|---|---|
| **P0 · harness 基础设施** | core/storage/llm/agent/session/tool/skill/context + CLI | `tsc` 全绿；冒烟全链路（建项目→editor→task 委派 writer→落盘）通过 | ✅ 已完成 |
| **P1 · 写作领域接入** | editor 设计对话驱动 docs/ 分层活文档；手动触发"写第 N 章"→ task 委派 规划(节拍)+写手(正文)；文风卡迁 SKILL.md | 真实项目里：从"一个想法"聊出 docs/ 四层 → 用户说写一章 → 出规划+正文落盘 | 端到端走通一次真实写作 |
| **P2 · 产品化** | Web/桌面 UI、上传提取文风、长篇状态层、token 订阅 | — | — |

---

## 7. 开放决策点

1. **Effect 去留**：已定不用 Effect（轻栈，见 04 §0/§12）。P0 已按此落地。
2. 已定：TS + Bun、文件系统存储先行；编辑器单主会话 + task 委派；无评判系统。具体见 04 与 02（2026-09-04 修订版）。

P0 完成，从 P1（写作领域接入）继续。
