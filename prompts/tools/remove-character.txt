Delete a character card (the file characters/<name>.md, under design/).

Before deleting, a reference check runs on the name across design/ and the hits are shown in
the confirmation — read the impact (other docs such as wiki/outline may reference this
character) before confirming.
