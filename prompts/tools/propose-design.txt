Present a draft of a design doc for the user to review. Nothing is saved and the turn ends, so they
can approve or ask for changes. This is the only way to write a design doc.

Name the target with layer (core | world | outline — the path is fixed by the tool) or, for any other
document, with name (topic pages, chapter plans, character cards).

Pass section to rewrite one section; omit it for a whole document. The draft must carry the layer's
own section headings (see design-spec) — a draft with no headings is refused, since the user would
be shown a blank page. Fields you cannot fill yet get （待定）.

Explain why you drafted it this way in your message BEFORE calling this, not after. Once the user
answers, apply-design lands it.
